Shared Pointer Lab — 简答题（人工评价，不计入自动评分的 100 分）
姓名：

Q1 [S1–S3] RAII 如何帮助异常路径释放资源？为什么引用计数必须由所有
共享同一对象的句柄共同访问，而不能是每个句柄独立的一份整数？
STUDENT TODO:

Q2 [S2] 比较 shared_ptr<int>{}、shared_ptr<int>{nullptr} 和
shared_ptr<int>{static_cast<int*>(nullptr)} 的 get()、bool、use_count()。
为什么只用 if (ptr) 判断是否需要处理控制块会出错？
STUDENT TODO:

Q3 [S4–S5] 复制、移动、自复制、自移动、同组的不同句柄移动赋值，
分别如何影响计数？std::move 本身是否移动了对象？
STUDENT TODO:

Q4 [S4–S6] 为什么先释放旧所有权，再读取 other，可能使 p = p->next
或 p = std::move(p->next) 访问已销毁的成员？描述安全的操作顺序。
STUDENT TODO:

Q5 [S2, S6–S7] 对象 new 成功、控制块 new 失败时谁负责释放对象？
reset(new T(...)) 要满足什么异常保证？本实验 make_shared 的两次分配
与标准库常见的合并分配实现有什么差别？
STUDENT TODO:

Q6 [S1] const shared_ptr<T> 与 shared_ptr<const T> 各自限制了什么？
为什么 operator*() const 在 T=int 时返回 int&？
STUDENT TODO:

Q7 [S8] 两条链表共享一个尾部时，哪些节点会在释放其中一条链表后
存活？请画出指针/所有权关系，并说明 use_count 不等于“可达路径数”。
STUDENT TODO:

Q8 [扩展思考，不要求编码] 引用计数为什么无法自动回收环？weak_ptr
如何解决这个问题？本实验为什么不是线程安全的？递归析构很长的
单链表还可能遇到什么限制？
STUDENT TODO:
